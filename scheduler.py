from apscheduler.schedulers.blocking import BlockingScheduler\n\n# Create a scheduler\nscheduler = BlockingScheduler()\n\n# Define the job to run trading bot\ndef run_trading_bot():\n    # Place your trading bot logic here\n    print('Running trading bot...')  # Placeholder for actual trading logic\n\n# Schedule the job to run every 30 minutes during market hours\n# Market hours: 09:30 to 16:00 (UTC-5) converted to UTC\nscheduler.add_job(run_trading_bot, 'cron', hour='14-20', minute='*/30')  # Adjusted for UTC\n\n# Start the scheduler\nscheduler.start()